#include "mesh.h"

bool Mesh::loadMesh(std::string filename)
{
    //load mesh from file
    return true;
}


void convert_mesh(std::string inputMesh)
{
    Mesh mesh;
    mesh.loadMesh(inputMesh);
}


